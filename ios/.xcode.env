#!/bin/sh
set -e

# Xcode bundling scripts (React Native / Expo) rely on NODE_BINARY.
# Keep this dynamic (not a hardcoded absolute path) so it works on CI too.
export NODE_BINARY="$(command -v node)"
