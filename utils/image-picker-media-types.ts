import type { MediaType } from 'expo-image-picker';

/** Текущий expo-image-picker: MediaType — литерал 'images' | 'videos' | 'livePhotos'. */
export function getImagePickerImagesMediaTypes(): MediaType[] {
  return ['images'];
}









