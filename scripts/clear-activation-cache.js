/**
 * Простой скрипт для очистки кэша кодов активации
 * 
 * ВАЖНО: Этот скрипт нужно запустить из приложения или через React Native Debugger
 * 
 * Для использования в приложении:
 * 1. Импортируйте функцию в любом экране:
 *    import { resetAllActivationKeys } from '@/utils/activationKeyValidator';
 * 
 * 2. Вызовите функцию:
 *    await resetAllActivationKeys();
 * 
 * Или используйте React Native Debugger:
 * - Откройте консоль в React Native Debugger
 * - Выполните:
 *   const { resetAllActivationKeys } = require('./utils/activationKeyValidator');
 *   resetAllActivationKeys().then(() => console.log('Коды сброшены!'));
 */

console.log('Для сброса кэша используйте функцию resetAllActivationKeys() из utils/activationKeyValidator.ts');

